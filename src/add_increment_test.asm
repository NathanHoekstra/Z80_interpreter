ld a, #0    ;load 0 into register a
ld b, #0    ;load 0 into register b
inc b       ;increment register b by 1
inc b       ;increment register b by 1
add a, #10  ;add 10 to register a
add a, b    ;add register b to register a