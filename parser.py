from typing import List
from lexer import Token
from helpers.rules import parser_rules


# parser :: List[Token] -> int -> List[List[Token]]
def parser(token_list: List[Token], current_line: int = 0) -> List[List[Token]]:
    
    pass
